<%= form_for(@true_answer) do |f| %>
  <% if @true_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@true_answer.errors.count, "error") %> prohibited this true_answer from being saved:</h2>

      <ul>
      <% @true_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :stats %><br />
    <%= f.text_field :stats %>
  </div>
  <div class="field">
    <%= f.label :steps %><br />
    <%= f.text_field :steps %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
